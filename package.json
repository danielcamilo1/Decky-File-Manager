{
  "name": "steamos-file-manager",
  "version": "1.0.0",
  "description": "Gerenciador de arquivos para do Decky",
  "type": "module",
  "scripts": {
    "build": "rollup -c",
    "package": "pnpm run build && rm -f steamos-file-manager.zip && rm -rf .tmp/steamos-file-manager && mkdir -p .tmp/steamos-file-manager && cp -r dist locales package.json plugin.json main.py README.md LICENSE .tmp/steamos-file-manager && cd .tmp && zip -r ../steamos-file-manager.zip steamos-file-manager && cd .. && rm -rf .tmp && echo 'steamos-file-manager.zip criado com sucesso.'",
    "watch": "rollup -c -w",
    "auto-package": "pnpm run build && chokidar \"src/**\" -c 'pnpm run package'",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "decky",
    "plugin",
    "steam-deck",
    "file-manager",
    "overlay"
  ],
  "author": "Ciphay",
  "license": "BSD-3-Clause",
  "bugs": {
    "url": "https://github.com/SteamDeckHomebrew/decky-plugin-template/issues"
  },
  "homepage": "https://github.com/SteamDeckHomebrew/decky-plugin-template#readme",
  "devDependencies": {
    "@decky/rollup": "^1.0.2",
    "@decky/ui": "^4.11.0",
    "@rollup/rollup-linux-x64-musl": "^4.53.3",
    "@types/react": "19.1.1",
    "@types/react-dom": "19.1.1",
    "@types/webpack": "^5.28.5",
    "rollup": "^4.53.3",
    "typescript": "^5.6.2",
    "chokidar-cli": "^3.0.0"
  },
  "dependencies": {
    "@decky/api": "^1.1.3",
    "tslib": "^2.7.0"
  },
  "pnpm": {
    "peerDependencyRules": {
      "ignoreMissing": [
        "react",
        "react-dom"
      ]
    }
  }
}
